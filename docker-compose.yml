services:
  medscan-ai:
    build: .
    ports:
      - "5000:5000"
    # Remove the volumes line for /app/models to use the baked version
    networks:
      - medscan-network

networks:
  medscan-network:
    driver: bridge